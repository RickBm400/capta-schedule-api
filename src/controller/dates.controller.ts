import type { DateFormatParams } from "../types/controller.ts";

export const getDateFormat = async (req: Request, res: Response) => {
    try {
        const params = req.params as unknown as any;
    } catch (error) {}
};
